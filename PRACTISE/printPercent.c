#include "main.h"

/**
 * printPercent - Print a percent symbol.
 * @arg: Argument (unused).
 *
 * Return: Always returns 1 (the number of characters printed).
 */
int printPercent(va_list arg __attribute__((unused)))
{
	_putchar('%');
	return (1);
}
